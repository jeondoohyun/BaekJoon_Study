import java.util.*

/**
 *  BaekJoon
 * */

// kotlin 클래스 파일이 아니라 kotlin file로 만들어야함

// 1000
fun main(args: Array<String>) = with(Scanner(System.`in`)){ // with을 사용하면 scan.nextInt()의 인스턴스 없이 바로 nextInt()만 사용가능
    println(nextInt()+nextInt())
}


